# Local agent / editor state
.freebuff/
.vscode/
.idea/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Python static-preview server artifacts
__pycache__/
*.pyc

# Internal planning notes (not part of the published website)
Chatgpt prompt.txt
Claude Prompt.txt

# Owner-facing to-do list. Kept private on purpose: it records the legal details
# that still need supplying, so it must not appear in the public repository.
LAUNCH-CHECKLIST.md
